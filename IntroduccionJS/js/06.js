// Objeto math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); //ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); //floor siempre redondea hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); //valor absoluto
resultado = Math.min(2, 3, 1, 50, -5); //el menor de todos
resultado = Math.max(2, 3, 1, 50, -5); //el mayor de todos
resultado = Math.random(); //numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); //numero aleatorio

console.log(resultado);